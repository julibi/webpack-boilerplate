import React from 'react';
import style from './Example.css';

const Example = () => {
  return(
    <div>
      <h2 className={ style.exampleHeader }>This is the example header</h2>
    </div>
  );
};

export default Example;
